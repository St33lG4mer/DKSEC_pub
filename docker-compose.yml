services:
  threat-hunter:
    build: .
    container_name: threat-hunter
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - threat-hunter-state:/data/state
      - ./reports:/data/reports
      - ./:/data/repo

volumes:
  threat-hunter-state:
